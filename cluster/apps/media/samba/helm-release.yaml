---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: samba
  namespace: media
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts/
      chart: samba
      version: 6.0.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home-charts
        namespace: flux-system
      interval: 5m
  values:
    env:
      # -- Set the container timezone
      TZ: ${TIMEZONE}
      SHARE1: Sleep;/sleep;yes;yes;yes # read-only
      #SHARE1: Sleep;/sleep;yes;no;yes # allow write
      PERMISSIONS: true

    service:
      main:
        type: LoadBalancer
        loadBalancerIP: "${SVC_SAMBA_ADDR}"
        ports:
          tcp:
            enabled: true
            protocol: TCP
            port: 445
          netbios:
            enabled: true
            protocol: TCP
            port: 139

    persistence:
      sleep:
        enabled: true
        existingClaim: samba-sleep

    resources:
      requests:
        cpu: 50m
        memory: 64Mi
      limits:
        cpu: 100m
        memory: 128Mi
